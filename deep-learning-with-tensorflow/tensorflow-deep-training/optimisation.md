For your convenience, we've included the rest of the computation graph:

* loss function
* optimiser
* statistics

Once the code is ready, you may want to tweak the hyperparameters, choose a different loss function or the optimiser.
